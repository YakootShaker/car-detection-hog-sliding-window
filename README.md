# car-detection-hog-sliding-window
An implementation of a classic computer vision object detection pipeline. This project uses Histogram of Oriented Gradients (HOG) features, a trained classifier, and a Sliding Window approach to detect vehicles using the UIUC Image Database for Car Detection.
